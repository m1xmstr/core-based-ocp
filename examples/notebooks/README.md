# Notebook Example

This example is a lightweight notebook-oriented flow for OpenShift AI or any Jupyter environment that can reach your serving endpoint.

The point is not to provide a full notebook export. The point is to show the shape of a small benchmark or validation loop you can adapt for your environment.
